### My example app to practice


## Boot instructions
Start rails server:
* bundle install
* bundle exec rails s

* [ **```Mysql must be installed```** ]

Start webpack:
* yarn install
* bin/webpacker-dev-server
